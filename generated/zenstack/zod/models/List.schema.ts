/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    title: z.string().min(1).max(100),
    private: z.boolean().default(false),
}
).strict();
const baseSchemaWithoutDefaults = z.object({
    id: z.string(),
    createdAt: z.coerce.date(),
    updatedAt: z.coerce.date(),
    title: z.string().min(1).max(100),
    private: z.boolean(),
}
).strict();
const relationSchema = z.object({
    space: z.record(z.string(), z.unknown()),
    owner: z.record(z.string(), z.unknown()),
    todos: z.array(z.unknown()).optional(),
}
);
const fkSchema = z.object({
    spaceId: z.string(),
    ownerId: z.string().optional(),
}
);

/**
 * `List` schema excluding foreign keys and relations.
 */
export const ListScalarSchema = baseSchema;


/**
 * `List` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const ListSchema = ListScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const ListPrismaCreateSchema = baseSchemaWithoutDefaults.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const ListPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date(),
    updatedAt: z.coerce.date(),
    title: z.string().min(1).max(100),
    private: z.boolean()
}).partial().passthrough();


/**
 * `List` schema for create operations excluding foreign keys and relations.
 */
export const ListCreateScalarSchema = baseSchemaWithoutDefaults.partial({
    id: true, createdAt: true, updatedAt: true, private: true
});


/**
 * `List` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const ListCreateSchema = ListCreateScalarSchema.merge(fkSchema);


/**
 * `List` schema for update operations excluding foreign keys and relations.
 */
export const ListUpdateScalarSchema = baseSchemaWithoutDefaults.partial();


/**
 * `List` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const ListUpdateSchema = ListUpdateScalarSchema.merge(fkSchema.partial());

