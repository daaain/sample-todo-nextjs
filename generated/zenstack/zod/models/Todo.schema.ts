/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    title: z.string().min(1).max(100),
    completedAt: z.coerce.date().nullish(),
}
).strict();
const baseSchemaWithoutDefaults = z.object({
    id: z.string(),
    createdAt: z.coerce.date(),
    updatedAt: z.coerce.date(),
    title: z.string().min(1).max(100),
    completedAt: z.coerce.date().nullish(),
}
).strict();
const relationSchema = z.object({
    owner: z.record(z.string(), z.unknown()),
    list: z.record(z.string(), z.unknown()),
}
);
const fkSchema = z.object({
    ownerId: z.string().optional(),
    listId: z.string(),
}
);

/**
 * `Todo` schema excluding foreign keys and relations.
 */
export const TodoScalarSchema = baseSchema;


/**
 * `Todo` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const TodoSchema = TodoScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const TodoPrismaCreateSchema = baseSchemaWithoutDefaults.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const TodoPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date(),
    updatedAt: z.coerce.date(),
    title: z.string().min(1).max(100),
    completedAt: z.coerce.date().nullish()
}).partial().passthrough();


/**
 * `Todo` schema for create operations excluding foreign keys and relations.
 */
export const TodoCreateScalarSchema = baseSchemaWithoutDefaults.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Todo` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const TodoCreateSchema = TodoCreateScalarSchema.merge(fkSchema);


/**
 * `Todo` schema for update operations excluding foreign keys and relations.
 */
export const TodoUpdateScalarSchema = baseSchemaWithoutDefaults.partial();


/**
 * `Todo` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const TodoUpdateSchema = TodoUpdateScalarSchema.merge(fkSchema.partial());

