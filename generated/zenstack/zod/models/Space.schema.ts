/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { SpaceUserRole } from '../../models';
const baseSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    name: z.string().min(4).max(50),
    slug: z.string().regex(new RegExp("^[0-9a-zA-Z]{4,16}$")),
}
).strict();
const baseSchemaWithoutDefaults = z.object({
    id: z.string(),
    createdAt: z.coerce.date(),
    updatedAt: z.coerce.date(),
    name: z.string().min(4).max(50),
    slug: z.string().regex(new RegExp("^[0-9a-zA-Z]{4,16}$")),
}
).strict();
const relationSchema = z.object({
    owner: z.record(z.string(), z.unknown()),
    members: z.array(z.unknown()).optional(),
    lists: z.array(z.unknown()).optional(),
}
);
const fkSchema = z.object({
    ownerId: z.string().optional(),
}
);

/**
 * `Space` schema excluding foreign keys and relations.
 */
export const SpaceScalarSchema = baseSchema;


/**
 * `Space` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const SpaceSchema = SpaceScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const SpacePrismaCreateSchema = baseSchemaWithoutDefaults.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const SpacePrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date(),
    updatedAt: z.coerce.date(),
    name: z.string().min(4).max(50),
    slug: z.string().regex(new RegExp("^[0-9a-zA-Z]{4,16}$"))
}).partial().passthrough();


/**
 * `Space` schema for create operations excluding foreign keys and relations.
 */
export const SpaceCreateScalarSchema = baseSchemaWithoutDefaults.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Space` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const SpaceCreateSchema = SpaceCreateScalarSchema.merge(fkSchema);


/**
 * `Space` schema for update operations excluding foreign keys and relations.
 */
export const SpaceUpdateScalarSchema = baseSchemaWithoutDefaults.partial();


/**
 * `Space` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const SpaceUpdateSchema = SpaceUpdateScalarSchema.merge(fkSchema.partial());

